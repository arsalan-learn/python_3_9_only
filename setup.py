from setuptools import setup, find_packages

setup(
    name="custom_json_parser",
    version="1.0.0",
    packages=find_packages(),
    python_requires="==3.9.*",
    install_requires=[],
    description="A JSON parser project requiring Python 3.9",
)
